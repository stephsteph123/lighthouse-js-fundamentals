const station = [
  [0, 8, 2],
  [-9, 3, 3]
]

function range() {
  const goodRange = []
  for (const unit of station) {
    const start = station[0]
    const stop = station[1]
    const step = station[2]
 {
        goodStations.push(station[0])

      }
    }
  return goodStations
}

//numbers.push(total)
//console.log(range(0, 10, 2));
//console.log(range(10, 30, 5));
//console.log(range(-5, 2, 3));

//some_array.push(some_value)

/*
const numbers = [0,10,2];

function sumArrayOfNumbers(start, stop, step) {
  let total = 0
  start = numbers[0];
  stop = numbers[1];
  step = numbers[2];
  for (var i = 0; i > 10; i++) {
    total = total + i
  }
  return total
}

console.log(sumArrayOfNumbers(numbers))
/*
const data = 
[1,2];

function range (start){
  console.log(start)
}

const data = 
[0,10,2];

let test = function data (start,stop,step) {
  let total = 0
  for (var i = 0; i < 10; i++) 
    total = total + i
return total
}
console.log(test(data))

const data = 
[0,10,2];

let test = function data (start,stop,step) 
console.log(start)

const numbers = [1,2];

function sumArrayOfNumbers(numberArray) {
  let total = 0;

  for (let number of numberArray) {
  total += number;
  }
  return total;
}

console.log(sumArrayOfNumbers(numbers))

//where I was at 10;27pm
const numbers = [0,10,2];

function range(start, stop, step) {
  let total = 0;
  start = numbers[0];
  stop = numbers[1];
  step = numbers[2];

  for (var i = start; i < stop; step) {
  total += numbers;
  }
  return total;
}

console.log(range(numbers))

//where I was at 11:17

range = 
[0, 10, 2];

function range() {
  const goodRange = []
  for (var i = 0; i < 10; i++) {
    i = i + 2
  }
  return 
}
console.log(range)

//got me far..

const stations = [
  [0, 8, 2],
  [-9, 3, 3]
]

function range() {
  const goodStations = []
  for (const station of stations) {
    const capacity = station[1]
    
    if (capacity >= 20) {


      const type = station [2]
      if (type === "school" || type === "community centre") {
        goodStations.push(station[0])

      }
    }
  }

  return goodStations
}

    */